# Milestone 16: Frontend Project Setup

## Status
- [x] Installed base dependencies (`lucide-react`, `clsx`, `react-router`)
- [x] Configured global styles and CSS variables in `src/index.css`
- [x] Set up global application layout with Sidebar and Navbar using vanilla CSS
- [x] Configured React Router in `src/App.tsx`

## Tasks & Deliverables
1. **Global CSS**: Update `src/index.css` with a robust design system using CSS variables (colors, spacing, typography).
2. **Core Utilities**: Add `src/lib/utils.ts` for styling utilities (`cn`).
3. **Layout Components**: Create `src/components/layout/Sidebar.tsx` and `Navbar.tsx` and their `.module.css`.
4. **Routing Setup**: Configure a basic router wrapper in `App.tsx`.
